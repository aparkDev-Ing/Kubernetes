apiVersion: v1
kind : Pod

metadata:
  name: legacy-pod

spec:
  containers:
    - name: legacy-container
      image: my-legacyserver
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 80